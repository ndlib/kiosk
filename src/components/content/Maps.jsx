
import React, { Component, PropTypes } from 'react';

class Maps extends Component {

    constructor(props, content) {
        super(props, content);
    }

    render() {
      return (
        <div>
          Some maps.
        </div>
      );
    }
}

export default Maps;
